Build jar:
mvn clean verify assembly:single

How-to run:
Change the config script according to your needs, important field is the host group.
The host group will specify which hosts ( defined in DB ) will actually be monitored from this instance.
Set the proper jdbc connection settings to connect to the database where databases to monitor are defined and host connections can be found.

execute run.sh script, this will start gatherer process. Logger writers to pgmon_java.log
